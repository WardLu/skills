interface:
  display_name: "Shadow Skill Publisher"
  short_description: "Prepare, resume, and track safe multi-channel Skill publishing"
  default_prompt: "Use $shadow-skill-publisher to validate, prepare, resume, or monitor local Skill publishing."

policy:
  allow_implicit_invocation: true
